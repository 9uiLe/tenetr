# レポート単独での判断可能性 — 受け入れ記録(Issue #23)

Human-only の受け入れ項目。根拠となる実物レポートと確認観点を提示し、ユーザーの承認をもって受け入れとする(「CI green なら PR マージ可」の包括承認の下で本 PR のマージを暫定承認と扱い、異議があれば再設計する)。

## 根拠物

- **docs/phase6/e2e-run/report.html** — 実 Before 画面への一気通貫 Run の実レポート(ブラウザで開いて単独確認可能)

## 確認観点(§21 Phase 5 完了条件)

1. **デザイナーが CLI を見ずに判断できるか** — レポートは自己完結 HTML(画像埋め込み・11 セクション)。CLI 出力への参照なしに、warn(初期表示の情報過多)と human_review 2 件(主操作の一意性・コンポーネント一貫性)の内容/根拠/修正提案が読める。
2. **Agent の説明がなくても問題箇所を理解できるか** — 各 finding は観測事実と判断を別欄で持ち、確信度を表示。修正提案は原則 ID に結び付く。
3. 表示原則(§17.2)の遵守 — Warn/Fail/Human Review を優先表示、pass は折り畳み、Pack/Harness のバージョン表示。

## 制限(既知)

- 実画面 Run は ui-snapshot 未提供のため画像領域ハイライトが出ない(モデルが evidence_regions を返した場合のみ描画)。合成 fixture の Run では領域描画をテスト済み(#21)。運用でアプリ側 capture が ui-snapshot を供給すると決定的 finding にも領域が付く。

## 判定

- [ ] ユーザー確認: レポートだけで修正方針が理解できる(否認の場合は Issue を reopen し、§17.2 の表示を再設計する)
