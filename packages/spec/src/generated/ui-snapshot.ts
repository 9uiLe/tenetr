/* generated by scripts/generate-types.mjs — DO NOT EDIT.
 * Source of truth: packages/spec/schemas/. Regenerate: pnpm --filter @tenetr/spec codegen */

/**
 * Normalized accessibility/UI metadata captured for one screen state (§16.3). Coordinates are fractions of the screen (origin top-left, 0..1). Absence of an optional field means the source tool could not provide it (never encode unknown as false).
 */
export interface UISnapshot {
  schema_version: string;
  scenario: string;
  screen: {
    width: number;
    height: number;
    scale?: number;
  };
  source: {
    tool: string;
    tool_version?: string;
    /**
     * Path of the preserved raw dump artifact this snapshot was normalized from; empty string when the tool emits normalized form natively.
     */
    raw_artifact: string;
  };
  elements: Element[];
}
export interface Element {
  id: string;
  role: "button" | "text" | "image" | "toggle" | "textfield" | "container" | "other";
  label?: string;
  identifier?: string;
  frame: {
    x: number;
    y: number;
    width: number;
    height: number;
  };
  enabled?: boolean;
  hittable?: boolean;
  selected?: boolean;
  traits?: string[];
  children?: Element[];
}
